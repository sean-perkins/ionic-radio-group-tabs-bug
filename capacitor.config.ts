import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-radio-group-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
